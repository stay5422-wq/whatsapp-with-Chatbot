# WhatsApp Inbox - دليل نظام البوت والأقسام

## 🤖 نظام البوت الذكي

### كيف يعمل البوت؟

البوت هو نظام رد آلي ذكي يتفاعل مع العملاء من خلال شجرة أسئلة وأجوبة محددة مسبقاً.

### شجرة الأسئلة

#### 1. القائمة الرئيسية (welcome)
```
مرحبًا بك في شركة المسار الساخن للسفر والسياحة
اختر الخدمة المطلوبة:
  ✈️ حجوزات
  ℹ️ استفسارات  
  ⚠️ شكاوى
```

#### 2. قسم الحجوزات
- **حجز وحدات** → يحول للقسم: `units`
- **حجز سيارات** → يحول للقسم: `cars`
- **باقة سياحية** → يحول للقسم: `tourism`
- **حفلات خارجية** → يحول للقسم: `events`

#### 3. قسم الاستفسارات
- **أسعار الرحلات** → يحول للقسم: `pricing`
- **مواعيد الرحلات** → يحول للقسم: `scheduling`
- **خدمات إضافية** → يحول للقسم: `services`

#### 4. قسم الشكاوى
- **مشكلة في الحجز** → يحول للقسم: `complaints`
- **مشكلة في الدفع** → يحول للقسم: `complaints`
- **مشكلة في الخدمة** → يحول للقسم: `complaints`

## 👥 نظام الصلاحيات والأقسام

### الحسابات المتاحة

| الحساب | كلمة المرور | الصلاحية | القسم | ما يراه |
|--------|-------------|----------|--------|---------|
| admin | admin123 | مدير | جميع الأقسام | كل المحادثات |
| units_agent | 123456 | موظف | الوحدات السكنية | محادثات الوحدات فقط |
| cars_agent | 123456 | موظف | السيارات | محادثات السيارات فقط |
| tourism_agent | 123456 | موظف | الباقات السياحية | محادثات الباقات فقط |
| complaints_agent | 123456 | موظف | الشكاوى | محادثات الشكاوى فقط |

### آلية عمل النظام

1. **العميل يبدأ محادثة جديدة**
   - البوت يرسل رسالة ترحيب
   - يعرض القائمة الرئيسية

2. **العميل يختار خدمة**
   - البوت يطرح أسئلة محددة
   - يجمع البيانات المطلوبة

3. **التحويل للقسم المناسب**
   - تُحدث حقل `department` في المحادثة
   - المحادثة تظهر للموظفين المعنيين فقط

4. **الموظف يرد**
   - يمكنه تعطيل البوت
   - يكمل المحادثة يدوياً
   - يمكنه إسناد المحادثة لموظف آخر

## 🔧 التخصيص

### إضافة أسئلة جديدة

عدّل ملف `lib/questionTree.ts`:

```typescript
"new_question_id": {
  "id": "new_question_id",
  "text": "نص السؤال هنا",
  "options": [
    {
      "id": "1",
      "label": "الخيار الأول",
      "emoji": "🔥",
      "nextQuestionId": "next_question",
      "department": "target_department"
    }
  ]
}
```

### إضافة قسم جديد

1. أضف القسم في `types/index.ts`:
```typescript
export type Department = 
  | 'all'
  | 'bookings'
  | 'new_department'; // قسم جديد
```

2. أضف مستخدم جديد في `lib/mockData.ts`:
```typescript
{
  id: '6',
  username: 'new_agent',
  password: '123456',
  name: 'موظف القسم الجديد',
  role: 'agent',
  department: 'new_department',
  isActive: true,
}
```

3. أضف اسم القسم بالعربية في `app/page.tsx`:
```typescript
const names: { [key: string]: string } = {
  // ...
  new_department: 'القسم الجديد',
};
```

## 📊 تدفق البيانات

```
عميل جديد
    ↓
البوت يرسل رسالة ترحيب
    ↓
العميل يختار خدمة
    ↓
البوت يطرح أسئلة
    ↓
جمع البيانات (collectedData)
    ↓
تحويل للقسم (department)
    ↓
الموظف المختص يستلم المحادثة
    ↓
الرد اليدوي أو الاستمرار مع البوت
```

## 🎯 أمثلة الاستخدام

### مثال 1: حجز وحدة سكنية
```
1. العميل يختار: حجوزات → حجز وحدات
2. البوت يطلب: المدينة، عدد الليالي، عدد النزلاء
3. العميل يكتب: "الرياض، 3 ليالي، 4 أشخاص"
4. البوت يحفظ البيانات ويحول للقسم: units
5. موظف الوحدات يستلم ويتابع
```

### مثال 2: شكوى
```
1. العميل يختار: شكاوى → مشكلة في الحجز
2. البوت يطلب تفاصيل المشكلة
3. العميل يشرح المشكلة
4. البوت يعطي رقم شكوى ويحول للقسم: complaints
5. موظف الشكاوى يستلم ويحل المشكلة
```

## 🔗 API Integration (جاهز للربط)

هيكل البيانات جاهز للربط مع API:

```typescript
// POST /api/conversations
{
  contactName: string,
  phoneNumber: string,
  department: Department,
  currentQuestionId: string,
  collectedData: object
}

// POST /api/messages
{
  conversationId: string,
  text: string,
  sender: 'agent' | 'user' | 'bot',
  timestamp: Date
}
```

---

🎉 **النظام الآن جاهز للاستخدام مع API الواتساب!**
