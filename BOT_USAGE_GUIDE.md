# دليل استخدام نظام البوت الآلي 🤖

## المشكلة التي تم حلها
تم تفعيل نظام البوت الآلي وإصلاح عرض خيارات الأسئلة.

## التغييرات المطبقة

### 1. تفعيل البوت بشكل افتراضي
```typescript
const [botEnabled, setBotEnabled] = useState(true); // ✅ مفعّل الآن
```

### 2. تهيئة البوت تلقائياً
عند فتح أي محادثة، سيتم:
- إرسال رسالة الترحيب من البوت بعد ثانية واحدة
- عرض الخيارات الرئيسية (حجوزات - استفسارات - شكاوى)

### 3. عرض خيارات الأسئلة
تظهر الخيارات كأزرار تفاعلية بعد كل رسالة من البوت:
```tsx
{bot && botEnabled && !bot.isWaitingForInput() && bot.renderOptions()}
```

### 4. معالجة النصوص المدخلة
عندما يطلب البوت إدخال نصي (مثل تفاصيل الحجز)، يتم:
- إخفاء الخيارات
- قبول النص من المستخدم
- حفظ البيانات في `collectedData`
- الانتقال للسؤال التالي

## كيفية الاستخدام

### 1. افتح التطبيق
```
http://localhost:3002
```

### 2. سجل الدخول
```
اسم المستخدم: admin
كلمة المرور: admin123
```

### 3. اختر محادثة
اضغط على أي محادثة من القائمة اليمنى

### 4. انتظر رسالة البوت الترحيبية
ستظهر بعد ثانية واحدة مع الخيارات:
- ✈️ حجوزات
- ℹ️ استفسارات  
- ⚠️ شكاوى

### 5. اختر خياراً
اضغط على أي زر لبدء المحادثة الآلية

## مثال على تدفق المحادثة

### تدفق الحجوزات:
```
1. البوت: مرحباً بك... اختر الخدمة
   ↓ [اضغط: حجوزات]

2. البوت: شكراً لاختيارك قسم الحجوزات ✈️
   البوت: أي نوع من الحجوزات تريد؟
   ↓ الخيارات:
      - 🏠 وحدات سكنية
      - 🚗 سيارات
      - 🏝️ باقات سياحية
      - 🎉 حفلات

3. [اختر: وحدات سكنية]
   البوت: من فضلك أخبرني بتفاصيل حجزك
   ↓ [اكتب: أريد شقة في القاهرة لمدة 3 أيام]

4. البوت: رائع! تم استلام طلبك:
   - التفاصيل: أريد شقة في القاهرة لمدة 3 أيام
   - تم إرسال طلبك للقسم المختص
   - سيتم التواصل معك قريباً ✅
```

## الخصائص المتقدمة

### 1. تعطيل/تفعيل البوت يدوياً
اضغط على أيقونة 🤖 في رأس المحادثة

### 2. أنواع الأسئلة

#### أ. أسئلة الاختيار (Multiple Choice)
```typescript
{
  "text": "اختر نوع الحجز:",
  "options": [
    { "label": "وحدات", "nextQuestionId": "units" },
    { "label": "سيارات", "nextQuestionId": "cars" }
  ]
}
```

#### ب. أسئلة النص الحر (Text Input)
```typescript
{
  "text": "من فضلك أخبرني بتفاصيل حجزك",
  "requiresInput": true,
  "nextStep": "confirmation"
}
```

### 3. تجميع البيانات
يتم حفظ جميع إجابات المستخدم في:
```typescript
conversation.collectedData = {
  "unit_details": "شقة في القاهرة لمدة 3 أيام",
  "car_type": "سيارة عائلية",
  // ... etc
}
```

### 4. تحديد القسم المسؤول
عند الانتهاء، يتم تحويل المحادثة للقسم المناسب:
```typescript
conversation.department = 'units' // أو 'cars', 'tourism', إلخ
```

## الأقسام المتاحة

1. **units** - الوحدات السكنية 🏠
2. **cars** - السيارات 🚗
3. **tourism** - الباقات السياحية 🏝️
4. **events** - الحفلات 🎉
5. **pricing** - الأسعار 💰
6. **scheduling** - المواعيد 📅
7. **services** - الخدمات 🛎️
8. **complaints** - الشكاوى ⚠️

## شجرة الأسئلة الكاملة

يوجد **70+ سؤال وإجابة** في ملف:
```
lib/questionTree.ts
```

تشمل:
- قائمة الحجوزات (4 أنواع)
- قائمة الاستفسارات (3 أنواع)
- قائمة الشكاوى (متعددة)
- أسئلة جمع البيانات
- رسائل التأكيد
- إعادة التوجيه

## استكشاف الأخطاء

### المشكلة: الخيارات لا تظهر
**الحل**: 
1. تأكد أن البوت مفعّل (🤖 أزرق في الرأس)
2. انتظر ثانية واحدة بعد فتح المحادثة
3. تحقق من Console للأخطاء

### المشكلة: البوت لا يرد
**الحل**:
1. تأكد أن `currentQuestionId` موجود في conversation
2. تحقق من questionTree في lib/questionTree.ts
3. انظر إلى Console للأخطاء

### المشكلة: البيانات لا تُحفظ
**الحل**:
1. تأكد أن `requiresInput: true` في السؤال
2. تحقق من `nextStep` موجود
3. انظر إلى `conversation.collectedData`

## الكود المهم

### تهيئة البوت
```typescript
useEffect(() => {
  if (bot && conversation && botEnabled) {
    bot.initializeBot();
  }
}, [conversation?.id, botEnabled]);
```

### إرسال رسالة البوت
```typescript
const bot = useChatBot({
  conversation,
  onSendBotMessage: (message) => {
    onSendMessage(message.text, 'bot');
  },
  onUpdateConversation: (updates) => {
    onUpdateConversation(conversation.id, updates);
  },
});
```

### عرض الخيارات
```typescript
const renderOptions = () => {
  const currentQuestion = getCurrentQuestion();
  return (
    <div className="flex flex-wrap gap-2">
      {currentQuestion.options.map((option) => (
        <button onClick={() => handleOptionSelect(option.id)}>
          {option.emoji} {option.label}
        </button>
      ))}
    </div>
  );
};
```

## الخلاصة

✅ البوت مفعّل بشكل افتراضي  
✅ الخيارات تظهر تلقائياً  
✅ المحادثة التفاعلية تعمل  
✅ جمع البيانات يعمل  
✅ تحديد الأقسام يعمل  
✅ 70+ سؤال وإجابة جاهزة  

---

**جاهز للاستخدام الآن! 🚀**
