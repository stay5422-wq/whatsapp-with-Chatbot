# إعداد Railway Volume لحفظ WhatsApp Session

## لماذا نحتاج Volume؟
Railway Volume يحفظ بيانات WhatsApp session بشكل دائم، حتى عند إعادة النشر أو إعادة التشغيل.
بدون Volume، ستحتاج لمسح QR Code في كل مرة يعيد فيها Railway تشغيل السيرفر.

## خطوات الإعداد:

### 1. إنشاء Volume على Railway:

1. افتح مشروعك على Railway Dashboard
2. اختر service الـ WhatsApp (adorable-abundance)
3. اذهب إلى تبويب **"Variables"** أو **"Settings"**
4. ابحث عن قسم **"Volumes"** في القائمة الجانبية
5. اضغط **"+ New Volume"**

### 2. إعداد Volume:

- **Mount Path:** `/data`
- **Name:** `whatsapp-session` (أو أي اسم تريده)
- اضغط **"Add"** أو **"Create"**

### 3. إضافة متغير البيئة:

بعد إنشاء Volume، Railway تلقائياً يضيف متغير `RAILWAY_VOLUME_MOUNT_PATH=/data`

إذا لم يُضاف تلقائياً:
1. اذهب لـ **Variables**
2. أضف:
   - **Name:** `RAILWAY_VOLUME_MOUNT_PATH`
   - **Value:** `/data`

### 4. إعادة النشر:

1. اذهب لـ **Deployments**
2. اضغط على آخر deployment
3. اضغط **"Redeploy"**

### 5. مسح QR Code مرة أخيرة:

بعد إعادة النشر:
1. افتح صفحة "ربط واتساب" في موقعك
2. امسح QR Code
3. **من الآن فصاعداً:** session هيتحفظ بشكل دائم! ✅

## التحقق من نجاح الإعداد:

بعد مسح QR Code:
1. جرب restart السيرفر من الموقع
2. إذا لم يطلب QR Code مرة أخرى = Volume شغال صح! 🎉
3. إذا طلب QR Code = راجع الخطوات أعلاه

## ملاحظات مهمة:

- ⚠️ Volume حجمه محدود في Railway (عادة 1GB مجاناً)
- ✅ Session WhatsApp صغير جداً (بضع MB فقط)
- 🔄 Volume يبقى حتى لو حذفت الـ deployment
- 🗑️ لحذف Volume: Settings → Volumes → Delete

## استكشاف الأخطاء:

### مشكلة: لا يزال يطلب QR Code بعد إعداد Volume

**الحل:**
1. تأكد من `RAILWAY_VOLUME_MOUNT_PATH=/data` موجود في Variables
2. تأكد من Mount Path في Volume = `/data`
3. تحقق من logs: ابحث عن `📁 Using session path: /data/.wwebjs_auth`
4. إذا رأيت المسار صحيح ولا يزال يطلب QR، احذف Volume وأعد إنشاءه

### مشكلة: Session تضيع بعد فترة

**السبب:** Railway قد يحذف Volume غير المستخدم أو الممتلئ
**الحل:** تأكد من عدم امتلاء Volume (تحقق من Usage في Dashboard)

## الدعم:

إذا واجهت مشاكل:
1. تحقق من logs على Railway: Deployments → View Logs
2. ابحث عن رسائل خطأ تحتوي على "auth" أو "session"
3. تأكد من أن Chromium يعمل بشكل صحيح

---

**تم إعداد الكود بالفعل ليستخدم Volume تلقائياً عند توفره!**
